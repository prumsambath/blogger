<h1><%= @article.title %></h1>
<p><%= @article.body %></p>

<%= link_to 'Delete', article_path(@article), method: :delete,
    data: { confirm: 'Sure to delete this article?' } %>
<%= link_to '<< Back to Article List', articles_path %>
